int8 main(char a, int8 s, int[] q)
{
	print(concatenate("Add", "strings"));
	if((s*2) + 243 > 1000)
	{
		print("Larger");
	}
	else
	{
		print("Smaller");
	}

	for(int8 i = 0; i < 10000; i++)
	{
		if(123 + 456 = (int8)"579") { sq(2); }
	}
}

char[] concatenate(char[] a, char[] b)
{
	char[] combined = a + b;
	return combined;
}

int8 sq(int8 num)
{
	return num*num;
}
